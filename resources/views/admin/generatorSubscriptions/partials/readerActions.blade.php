


        <div class="d-flex align-items-center">
        <a href="#" class="nav-link edit_reding me-2"data-generator_subscription_id="{{$data->generator_subscription_id}}" data-reading_id="{{ $data->id }}">
            <i class="fas fa-edit text-primary"></i>
        </a>


                 <a href="#" id="{{ $data->id }}" name_delete="{{ $data->generatorSubscriptions?->name }}" class="nav-link reading_delete">
                    <i class="fa fa-trash text-danger"></i>  
    </div>
